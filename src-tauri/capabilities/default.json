{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capability for Dataset Inspector",
  "platforms": ["windows", "macOS", "linux"],
  "windows": ["main"],
  "webviews": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "store:default"
  ]
}
