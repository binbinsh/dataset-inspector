name: libsmbclient_ffi
description: >
  Dart FFI bindings for libsmbclient (Samba).
  Provides full SMB3 + multichannel file I/O at ~600+ MB/s throughput.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  ffi: ^2.1.0
