# Development

## Prerequisites

- Node.js 20+
- Rust toolchain

## Commands

- Install deps: `npm install`
- Dev (web + Tauri): `npm run tauri dev`
- Build web export: `npm run build` (outputs static site to `out/`)
- Build desktop app: `npm run tauri build`
